$(document).ready(function () {
  // Scroll
  $('.accordion__panel').beefup({
    scroll: true,
    openSingle: true,
    openSpeed: 400,
    closeSpeed: 400,
    selfClose: true,
    scrollOffset: -10
  });

});
  
